Para interpretar codigo fuente, primero necesitamos una herramienta para ejecutarlo, en este caso Python, que tiene el código de texto llamado IDLE en el cual se puede escribir, modificar y guardar todo el código que se escriba.
 
 Una vez hayas escrito el código correspondiente, presionamos F5 y automaticamente, se ejecutará en el mismo programa.ç
 
Otra manera de ejecutarlo es; accediendo desde terminal
abres terminal y escribes la ruta correspondiente a donde esté el archivo guardado, y desde ahi tecleas el nombre del archivo y automaticamente te sale lo que ha escrito.
 
 
 En este caso : print("Hola mundo") es un comando básico y muy facil de ejecutar, para principalmente, saber como funciona el programama y también para que funciona el comando "print"
 
 Todos los ejercicios o codigo escritos deben guardarese en .py, ya que Python esd la herramienta de programación que estamos guardando.
