Las constantes son valores que no pueden ser modificados durante la ejecución.  A diferencia de las variables, cuyo valor puede variar, el valor de una constante permanece fijo e inmutable.

La constante se va a llamar:
TIEMPO_BUCEO

En esta carrera participan varios jugadores, lo cual lo vamos a representar:
Numero_jugadores=7

Para calcular el cuanto tiempo tardarían 7 jugadores en realizar el buceo cada uno, sería tal que así:

tiempo_total = TIEMPO_BUCEO * NUMERO_JUGADORES

Para completar las operaciones en consola, tecleamos:

print("La duración estándar de un buceo es (TIEMPO_BUCEO) segundos.")
print("Para (NUMERO_JUGADORES) jugadores, el tiempo total es: (tiempo_total) segundos.")


Al darle un dato a cada variable, he asignado los pilares fundamentales del manejo de datos: variables y constantes
